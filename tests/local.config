// extra settings to use local execution intead of LSF, etc.

executor {
    $local {
        cpus = 24
        // queueSize = 4
        memory = '48 GB'
    }
}

process {
    executor = "local"
}

params {
    max_memory = "48.GB"
}
