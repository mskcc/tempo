/*
    Test config for somatic-only run (SNV + SV)
    Extends test.config, enables somatic calling, disables everything else
*/

params {
    // Enable somatic SNV + SV calling only
    workflows              = 'snv,sv'
    assay_type             = 'exome'
    skip_multiqc           = true
}
